"use client";
import React from 'react'

const Button = () => {
  return (
    <button className="btn btn-primary" type="button" onClick={()=>console.log('')}>Click</button>
  )
}

export default Button